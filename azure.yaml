# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: pulse
services:  
  app:
    language: dotnet
    project: ./src/Application.Aspire.AppHost/Application.Aspire.AppHost.csproj
    host: containerapp
